# HANDOFF — <routine name> (branch <auto/...>)

Session-level checkpoint so the next cold fire resumes exactly where this one
stopped. Copy this template to `autoresearch/HANDOFF.md` on your branch and keep it
current. Read it FIRST at session start; rewrite it before the session ends.

_Last updated: <YYYY-MM-DD HH:MM UTC> by <routine>_

## Current task
<the one thing in progress, e.g. "validating candidate C3 (ent_coef=0.03) on full panel">

## Progress / exact state
<e.g. "v26 panel: 6/10 stocks done (RELIANCE..SBIN committed); models_v26 gitignored on this branch">
<git is the data checkpoint; note here anything git alone doesn't make obvious>

## Immediate next step
<the very next action the next session should take, e.g. "run stock #7 HINDALCO, then readout">

## Do NOT re-do / gotchas
<things already tried this cycle, dead ends, flaky steps, partial writes to clean up>

## Frontier state at handoff
<current champion you were building from, so the next session confirms it didn't move>
